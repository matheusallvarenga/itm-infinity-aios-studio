/**
 * itm-infinity-aios-studio — SDK entry point (headless)
 *
 * This module will export the headless SDK (clients, hooks, state managers)
 * for programmatic consumption of the AIOS framework in Phase 2.
 *
 * Phase 1 scaffolding — placeholder only.
 */
export {};
